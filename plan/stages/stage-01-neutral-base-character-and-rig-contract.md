# Stage 1: Neutral Base Character and Rig Contract

## Goal
Create the approved neutral base character as a riggable SVG and validate the rig.

## Required Work
- Create assets/base-character.svg only after concept approval.
- Use viewBox 0 0 100 100.
- Add all required rig IDs.
- Add hidden effect layers.
- Add prompt placeholders, not the full prompt runtime.
- Implement CHARACTER_CONTRACT, REQUIRED_RIG_IDS, validateCharacterRig(), assertRigSlotExists(), and loadCharacterRig().
- Add unit tests for rig validation.

## Stop Condition
Stop and request user approval for the neutral base SVG and rig contract before Stage 2.

